<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.spring.demo.dao.ExampleMapper" >
  <resultMap id="BaseResultMap" type="com.spring.demo.model.Example" >
    <id column="id" property="id" jdbcType="INTEGER" />
    <result column="did" property="did" jdbcType="VARCHAR" />
    <result column="stuName" property="stuname" jdbcType="VARCHAR" />
    <result column="stuAge" property="stuage" jdbcType="VARCHAR" />
    <result column="stuSex" property="stusex" jdbcType="VARCHAR" />
  </resultMap>
  <sql id="Base_Column_List" >
    id, did, stuName, stuAge, stuSex
  </sql>
  <select id="selectByPrimaryKey" resultMap="BaseResultMap" parameterType="java.lang.Integer" >
    select 
    <include refid="Base_Column_List" />
    from example
    where id = #{id,jdbcType=INTEGER}
  </select>
  <delete id="deleteByPrimaryKey" parameterType="java.lang.Integer" >
    delete from example
    where id = #{id,jdbcType=INTEGER}
  </delete>
  <insert id="insert" parameterType="com.spring.demo.model.Example" >
    insert into example (id, did, stuName, 
      stuAge, stuSex)
    values (#{id,jdbcType=INTEGER}, #{did,jdbcType=VARCHAR}, #{stuname,jdbcType=VARCHAR}, 
      #{stuage,jdbcType=VARCHAR}, #{stusex,jdbcType=VARCHAR})
  </insert>
  <insert id="insertSelective" parameterType="com.spring.demo.model.Example" >
    insert into example
    <trim prefix="(" suffix=")" suffixOverrides="," >
      <if test="id != null" >
        id,
      </if>
      <if test="did != null" >
        did,
      </if>
      <if test="stuname != null" >
        stuName,
      </if>
      <if test="stuage != null" >
        stuAge,
      </if>
      <if test="stusex != null" >
        stuSex,
      </if>
    </trim>
    <trim prefix="values (" suffix=")" suffixOverrides="," >
      <if test="id != null" >
        #{id,jdbcType=INTEGER},
      </if>
      <if test="did != null" >
        #{did,jdbcType=VARCHAR},
      </if>
      <if test="stuname != null" >
        #{stuname,jdbcType=VARCHAR},
      </if>
      <if test="stuage != null" >
        #{stuage,jdbcType=VARCHAR},
      </if>
      <if test="stusex != null" >
        #{stusex,jdbcType=VARCHAR},
      </if>
    </trim>
  </insert>
  <update id="updateByPrimaryKeySelective" parameterType="com.spring.demo.model.Example" >
    update example
    <set >
      <if test="did != null" >
        did = #{did,jdbcType=VARCHAR},
      </if>
      <if test="stuname != null" >
        stuName = #{stuname,jdbcType=VARCHAR},
      </if>
      <if test="stuage != null" >
        stuAge = #{stuage,jdbcType=VARCHAR},
      </if>
      <if test="stusex != null" >
        stuSex = #{stusex,jdbcType=VARCHAR},
      </if>
    </set>
    where id = #{id,jdbcType=INTEGER}
  </update>
  <update id="updateByPrimaryKey" parameterType="com.spring.demo.model.Example" >
    update example
    set did = #{did,jdbcType=VARCHAR},
      stuName = #{stuname,jdbcType=VARCHAR},
      stuAge = #{stuage,jdbcType=VARCHAR},
      stuSex = #{stusex,jdbcType=VARCHAR}
    where id = #{id,jdbcType=INTEGER}
  </update>
  
  <!--  =============================== sql  =============================== 例子 -->
  
   <!-- 对象或map数据的插入 -->

    <!--
        向数据库中插入数据
    -->
    <insert id="insertObject" parameterType="com.spring.demo.model.Example">
        insert into Student
        (did,stuName,stuAge,stuSex)
        values
        (#{did}, #{stuName}, #{stuAge}, #{stuSex})
    </insert>


    <!--
      向数据库中批量的插入数据
   -->
    <insert id="insertBatchObjectList" parameterType="com.spring.demo.model.Example">
        insert into Student
        (did,stuName,stuAge,stuSex)
        values
        <foreach item="item" collection="list" separator=",">
            (#{item.did}, #{item.stuName}, #{item.stuAge}, #{item.stuSex})
        </foreach>
    </insert>



    <!--
       向数据库中插入数据
   -->
    <insert id="insertMapper" parameterType="java.util.HashMap">
        insert into Student
        (did,stuName,stuAge,stuSex)
        values
        (#{did}, #{stuName}, #{stuAge}, #{stuSex})
    </insert>


    <!--
       向数据库中批量的插入数据
    -->
    <insert id="insertBatchMapperList" parameterType="java.util.HashMap">
        insert into Student
        (did,stuName,stuAge,stuSex)
        values
        <foreach item="item" collection="list" separator=",">
            (#{item.did}, #{item.stuName}, #{item.stuAge}, #{item.stuSex})
        </foreach>
    </insert>


    <!-- 对象或map数据的插入 -->


    <!--
        根据id查询得到一个Example对象
     -->

    <!-- 单个对象的查询 -->

    <select id="selectObjectByPK" parameterType="String" resultType="com.spring.demo.model.Example">
        select * from Student where did=#{pk}
    </select>

    <select id="selectObjectBatchByPKList" parameterType="String" resultType="com.spring.demo.model.Example">
        select * from Student
        <where>
            did in
            <foreach collection="list" item="item"  open="(" separator="," close=")">
                #{item}
            </foreach>
        </where>
    </select>


    <select id="selectMapperByPK" parameterType="String" resultType="java.util.HashMap">
        select did,stuName,stuAge,stuSex from Student where did=#{pk}
    </select>



    <select id="selectObjectByObject" parameterType="com.spring.demo.model.Example" resultType="com.spring.demo.model.Example">
        select * from Student
        <where>
            <if test="stuName != null">
                and stuName=#{stuName}
            </if>
            <if test="stuAge != null">
                and stuAge=#{stuAge}
            </if>
        </where>
    </select>


    <select id="selectMapperByParams" parameterType="java.util.HashMap" resultType="java.util.HashMap">
        select did,stuName,stuAge,stuSex from Student
        <where>
            <if test="stuName != null">
                and stuName=#{stuName}
            </if>
            <if test="stuAge != null">
                and stuAge=#{stuAge}
            </if>
        </where>
    </select>


    <select id="selectSingleMapperByParams" parameterType="java.util.HashMap" resultType="java.util.HashMap">
        select did,stuName,stuAge,stuSex from Student
        <where>
            <if test="did != null">
                and did=#{did}
            </if>
        </where>
    </select>




    <select id="selectMapperByObject" parameterType="java.util.HashMap" resultType="java.util.HashMap">
        select did,stuName,stuAge,stuSex from Student
        <where>
            <if test="stuName != null">
                and stuName=#{stuName}
            </if>
            <if test="stuAge != null">
                and stuAge=#{stuAge}
            </if>
        </where>
    </select>



    <select id="selectObjectByParams" parameterType="java.util.HashMap" resultType="com.spring.demo.model.Example">
        select did,stuName,stuAge,stuSex from Student
        <where>
            <if test="stuName != null">
                and stuName=#{stuName}
            </if>
            <if test="stuAge != null">
                and stuAge=#{stuAge}
            </if>
        </where>
    </select>


    <!-- 单个对象的查询 -->



    <select id="selectAllObject" resultType="com.spring.demo.model.Example">
        select * from Student
    </select>


    <select id="selectAllObjectByObject" resultType="com.spring.demo.model.Example">
        select * from Student
        <where>
            <if test="stuSex != null and stuSex != ''">
                stuSex=#{stuSex}
            </if>
        </where>
    </select>


    <select id="selectAllObjectByMapper" resultType="com.spring.demo.model.Example">
        select * from Student
        <where>
            <if test="stuSex != null and stuSex != ''">
                stuSex=#{stuSex}
            </if>
        </where>
    </select>

    <select id="selectAllMapper" resultType="java.util.HashMap">
        select * from Student
    </select>


    <select id="selectAllMapperByObject" resultType="java.util.HashMap">
        select * from Student
        <where>
            <if test="stuSex != null and stuSex != ''">
                stuSex=#{stuSex}
            </if>
        </where>
    </select>


    <select id="selectAllMapperByMapper" resultType="java.util.HashMap">
        select * from Student
        <where>
            <if test="stuSex != null and stuSex != ''">
                stuSex=#{stuSex}
            </if>
        </where>
    </select>


    <!-- 不用在这里实现分页的方法, 通过map进行数据查询 -->
    <select id="selectPageObjectByMapper" resultType="java.util.HashMap">
        select * from Student
        <where>
            <if test="stuSex != null and stuSex != ''">
                stuSex=#{stuSex}
            </if>
        </where>

    </select>


    <!-- 不用在这里实现分页的方法, 通过对象进行对象查询 -->
    <select id="selectPageObjectByObject" resultType="com.spring.demo.model.Example">
        select * from Student
        <where>
            <if test="stuSex != null and stuSex != ''">
                stuSex=#{stuSex}
            </if>
        </where>

    </select>


    <!-- 不用在这里实现分页的方法 -->
    <select id="selectPageMapperByMapper" resultType="java.util.HashMap">
        select * from Student
        <where>
            <if test="stuSex != null and stuSex != ''">
                stuSex=#{stuSex}
            </if>
        </where>

    </select>


    <!-- 不用在这里实现分页的方法, 通过对象进行对象查询 -->
    <select id="selectPageMapperByObject" resultType="com.spring.demo.model.Example">
        select * from Student
        <where>
            <if test="stuSex != null and stuSex != ''">
                stuSex=#{stuSex}
            </if>
        </where>
    </select>

    <!-- 统一框架,对数据进行修改 -->


    <update id="updateObjectByObject" parameterType="com.spring.demo.model.Example">
        update student
        <set>
            <if test="stuName != null and stuName != ''">
                stuName=#{stuName}
            </if>
        </set>
        <where>
            <if test="did != null and did != '' ">
                did = #{did}
            </if>
        </where>
    </update>


    <update id="updateObjectBatchByObjectList" parameterType="java.util.ArrayList">
        <foreach collection="list" item="item" index="index"  separator=";">
            update Student
            <set>
                <if test="item.stuName != null and item.stuName != ''">
                    stuName=#{item.stuName}
                </if>
            </set>
            <where>
                <if test="item.did != null and item.did != ''">
                    did=#{item.did}
                </if>
            </where>
        </foreach>
    </update>



    <update id="updateObjectByMapper" parameterType="java.util.HashMap">
        update Student
        <set>
            <if test="stuName != null and stuName != ''">
                stuName=#{stuName}
            </if>
        </set>
        <where>
            <if test="did != null and did != '' ">
                did = #{did}
            </if>
        </where>
    </update>


    <update id="updateObjectBatchByMapperList" parameterType="java.util.ArrayList">
        <foreach collection="list" item="item" separator=";">
            update Student
            <set>
                <if test="item.stuName != null and item.stuName != ''">
                    stuName=#{item.stuName}
                </if>
            </set>
            <where>
                <if test="item.did != null and item.did != '' ">
                    did = #{item.did}
                </if>
            </where>
        </foreach>
    </update>


    <!-- 统一框架,对数据进行修改 -->



    <!-- 数据删除操作 -->


    <delete id="deleteObjectByPK" parameterType="String" >
        delete from Student where did = #{did}
    </delete>

    <delete id="deleteBatchObjectByPK" parameterType="java.util.ArrayList" >
        delete from Student
        <where>
            did in
            <foreach collection="list" item="item"  open="(" separator="," close=")">
                #{item}
            </foreach>
        </where>
    </delete>


    <delete id="deleteObjectByObject" parameterType="com.spring.demo.model.Example" >
        delete from Student
        <where>
            <if test="did != null and did !='' ">
                did = #{did}
            </if>
        </where>
    </delete>

    <delete id="deleteObjectByMapper" parameterType="java.util.HashMap" >
        delete from Student
        <where>
            <if test="did != null and did !='' ">
                did = #{did}
            </if>
        </where>
    </delete>

    <delete id="deleteObjectBatchByObjectList" parameterType="com.spring.demo.model.Example" >
        delete from Student
        <where>
            did in
            <foreach collection="list" item="item"  open="(" separator="," close=")">
                #{item.did}
            </foreach>
        </where>
    </delete>

    <delete id="deleteObjectBatchByMapperList" parameterType="java.util.ArrayList" >
        delete from Student
        <where>
            did in
            <foreach collection="list" item="item"  open="(" separator="," close=")">
                #{item.did}
            </foreach>
        </where>
    </delete>
    <!-- 数据删除操作 -->
  <!--  =============================== sql  =============================== 例子 -->
  
</mapper>