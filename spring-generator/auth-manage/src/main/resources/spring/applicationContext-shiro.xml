<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-3.1.xsd"
	default-lazy-init="true">

	<description>Shiro安全配置</description>
	
	<!-- Shiro's main business-tier object for web-enabled applications -->
	<bean id="securityManager" class="org.apache.shiro.web.mgt.DefaultWebSecurityManager">
		<!-- 管理认证和授权 -->
		<property name="realm" ref="shiroRealm" />
		<!-- shiro 缓存管理 -->
		<property name="cacheManager" ref="shiroEhcacheManager" />
		<!-- 自动登录使用,暂时没有使用
        <property name="rememberMeManager" ref="rememberMeManager" /> -->
        <!-- 分布式或单机session缓存- 暂时没有使用
        <property name="sessionManager" ref="sessionManager" /> -->
	</bean>

	<!-- 項目自定义的Realm  -->
	<bean id="shiroRealm" class="com.auth.shiro.ShiroRealm"></bean>

	<!-- Shiro Filter -->
	<bean id="shiroFilter" class="org.apache.shiro.spring.web.ShiroFilterFactoryBean">
		<property name="securityManager" ref="securityManager" />
		<property name="loginUrl" value="/" /><!-- 没有登录的用户请求需要登录的页面时自动跳转到登录页面 -->
		<property name="unauthorizedUrl" value="/" /><!-- 没有权限默认跳转的页面 index.jsp-->
		<property name="successUrl" value="/login" /><!-- 登录成功默认跳转页面，不配置则跳转至”/”  -->
        <property name="filters">
	        <map> 
	        	 <entry key="authc" value-ref="consumerFormAuthenticationFilter"/>
	        	 <!-- 是否启用验证码检验 -->
	        	 <!-- 
	             <entry key="authc" value-ref="captchaFormAuthenticationFilter"/>
	              -->
	               <!-- 表单中的参数固定写法是username、password、rememberMe。
					       可以通过<property name="usernameParam" value="loginName" />来设置 
	              <entry key="authc">
                    <bean class="com.peter.smallshow.security.CustomFormAuthenticationFilter">  
                        <property name="usernameParam" value="loginName" />  
                        <property name="passwordParam" value="password" />  
                    </bean>  
                </entry>  --> 
                
	        </map>
        </property>
		<!-- 认证过滤配置 -->
		<property name="filterChainDefinitions">
			<ref bean="shiroFilterChainDefinitions" />
		</property>
	</bean>
	<!-- Shiro权限过滤器定义 -->
	<bean name="shiroFilterChainDefinitions" class="java.lang.String">
		<constructor-arg>
			<!-- anon表示匿名访问（不需要认证与授权）；
				 authc需要认证；authc, roles[admin]满足角色的认证
				 perms[SECURITY_ACCOUNT_VIEW] 表示用户需要提供值为“SECURITY_ACCOUNT_VIEW”Permission 信息
				 由此可见，连接地址配置为 authc 或 perms[XXX] 表示为受保护资源
			 -->
			<value>
				/html/** = anon
				/static/** = anon
				/login/timeout = anon
				/logout = logout
				/createImage = anon
				/checkLogin = anon
				/treeView = anon
				/login = anon
				/admin/** = authc, roles[admin]
		    	/** = authc
		 	</value>
		</constructor-arg>
	</bean> 
            
	<!-- 用户授权信息Cache, 采用EhCache -->
	<bean id="shiroEhcacheManager" class="org.apache.shiro.cache.ehcache.EhCacheManager">
		<property name="cacheManagerConfigFile" value="classpath:ehcache-shiro.xml"/>
	</bean>

	<!-- 保证实现了Shiro内部lifecycle函数的bean执行 -->
	<bean id="lifecycleBeanPostProcessor" class="org.apache.shiro.spring.LifecycleBeanPostProcessor"/>

    
	<!-- 验证码检验 filter
	<bean id="captchaFormAuthenticationFilter" class="com.auth.shiro.CaptchaFormAuthenticationFilter"/> -->
	
	
	<bean id="consumerFormAuthenticationFilter" class="com.auth.shiro.ConsumerFormAuthenticationFilter"/>
	
	
	<!-- AOP式方法级权限检查   这两个类主要用于注解 ,开启Shiro注解的Spring配置方式的beans。在lifecycleBeanPostProcessor之后运行-->
    <bean class="org.springframework.aop.framework.autoproxy.DefaultAdvisorAutoProxyCreator" depends-on="lifecycleBeanPostProcessor"/>
    <bean class="org.apache.shiro.spring.security.interceptor.AuthorizationAttributeSourceAdvisor">
        <property name="securityManager" ref="securityManager"/>
    </bean>
    
	<!-- 指定本系统SESSIONID, 默认为: JSESSIONID 问题: 与SERVLET容器名冲突, 如JETTY, TOMCAT 等默认JSESSIONID,
	当跳出SHIRO SERVLET时如ERROR-PAGE容器会为JSESSIONID重新分配值导致登录会话丢失! -->
    <bean id="sessionIdCookie" class="org.apache.shiro.web.servlet.SimpleCookie">
        <constructor-arg name="name" value="web.session.id" />
    </bean>

	<!-- rememberMe管理器 -->
	<bean id="rememberMeManager" class="org.apache.shiro.web.mgt.CookieRememberMeManager">
		<!-- rememberMe cookie加密的密钥 建议每个项目都不一样 默认AES算法 密钥长度（128 256 512 位） -->
		<property name="cipherKey" value="#{T(org.apache.shiro.codec.Base64).decode('4AvVhmFLUs0KTA3Kprsdag==')}" />
		<property name="cookie" ref="rememberMeCookie" />
	</bean>
	<bean id="rememberMeCookie" class="org.apache.shiro.web.servlet.SimpleCookie">
		<constructor-arg value="rememberMe" />
		<property name="httpOnly" value="true" />
		<property name="maxAge" value="2592000" /><!-- 30天 -->
    </bean>
</beans>